block/SyscfgSCfgns:
  description: SYSCFG_S_CFGNS peripheral.
  items:
  - name: cfgnstcalib
    description: Configure to define the system tick for the M33.
    byte_offset: 28
    fieldset: regs::Cfgnstcalib
  - name: rootnsdata0
    description: Generic data space for user to pass to root, e.g., address of struct in mem.
    byte_offset: 1536
    fieldset: regs::Rootnsdata0
  - name: rootnsdata1
    description: Generic data space for user to pass to root, e.g., address of struct in mem.
    byte_offset: 1540
    fieldset: regs::Rootnsdata1
fieldset/regs::Cfgnstcalib:
  description: Configure to define the system tick for the M33.
  fields:
  - name: tenms
    description: Ten Milliseconds.
    bit_offset: 0
    bit_size: 24
  - name: skew
    description: Skew.
    bit_offset: 24
    bit_size: 1
  - name: noref
    description: No Reference.
    bit_offset: 25
    bit_size: 1
fieldset/regs::Rootnsdata0:
  description: Generic data space for user to pass to root, e.g., address of struct in mem.
  fields:
  - name: data
    description: Data.
    bit_offset: 0
    bit_size: 32
fieldset/regs::Rootnsdata1:
  description: Generic data space for user to pass to root, e.g., address of struct in mem.
  fields:
  - name: data
    description: Data.
    bit_offset: 0
    bit_size: 32
